#include <stdio.h>

int ara[100];
int start, end;

int main()
{
    int i, j, k;

    start = end = 1;
    int op;
    option;
    while(1){
        scanf("%d", &op);

        if(op == 1){
            printf("Enter push value\n");
        }
        else if(op == 2)
    }

    return 0;
}
