#include <stdio.h>

int main()
{
    int i, j, n, ara[100];

    scanf("%d", &n);

    for(i = 0; i < n; i++)
    {
        scanf("%d", &ara[i]);
    }

    for(i = 0; i< n; i++)
    {
        if
    }

    return 0;

}
