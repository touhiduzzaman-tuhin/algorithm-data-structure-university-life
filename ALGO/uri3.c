#include <stdio.h>

int main()
{
    int a, b, c;

    scanf("%d %d %d", &a, &b, &c);

    if(a > b && b > c)
    {
        printf("%d\n%d\n%d\n\n%d\n%d\n%d\n", c, b, a, a, b, c);

    }

    else if(a > b && a > c)

    return 0;
}
