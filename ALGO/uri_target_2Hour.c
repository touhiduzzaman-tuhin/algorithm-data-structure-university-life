#include <stdio.h>

int main()
{
    int a, b, c, d, n, m, p;

    double x, y, z;

    scanf("%d %d %lf", &a, &b, &x);

    printf("MEMBER = %d\n", a);

    y = b * x;

    printf("SALARY = U$ %.2lf\n", y);

    return 0;
}
